function loginService(data) {
  return {
    message: 'Not connected to login endpoint yet. TODO!!!.',
    data,
  };
}

function registerService(data) {
  return {
    message: 'Not connected to register endpoint yet. TODO!!!.',
    data,
  };
}

function resetPassword(data) {
  return {
    message: 'Not connected to reset password yet.. TODO!!!!',
    data,
  };
}

module.exports = {
  loginService,
  registerService,
  resetPassword,
};
